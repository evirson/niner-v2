CREATE TABLE TIPO_CARTEIRA (
   ID_CARTEIRA INTEGER GENERATED BY DEFAULT AS IDENTITY,
   NOME_CARTEIRA VARCHAR(50) NOT NULL,
   PRAZO_PAGAMENTO INTEGER NOT NULL,
   PC_MINIMA INTEGER NOT NULL,
   PC_MAXIMA INTEGER NOT NULL,
   TAXA_ADMINISTRADORA FLOAT NOT NULL);
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

ALTER TABLE TIPO_CARTEIRA ADD CONSTRAINT PK_TIPO_CARTEIRA PRIMARY KEY (ID_CARTEIRA);
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------


