CREATE TABLE CAIXA_MESTRE (
   ID_CAIXA INTEGER GENERATED BY DEFAULT AS IDENTITY,
   ID_EMPRESA INTEGER NOT NULL,
   ID_USUARIO INTEGER NOT NULL,
   DATA_ABERTURA TIMESTAMP NOT NULL,
   DATA_FECHAMENTO TIMESTAMP,
   SALDO_INCIAL FLOAT DEFAULT 0,
   SALDO_FINAL FLOAT DEFAULT 0,
   CAIXA_FECHADO VARCHAR(1) NOT NULL CHECK (CAIXA_FECHADO IN ('S', 'N')),
   OBSERVACOES VARCHAR(1000));
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

ALTER TABLE CAIXA_MESTRE ADD CONSTRAINT PK_CAIXA_MESTRE PRIMARY KEY (ID_CAIXA);

ALTER TABLE CAIXA_MESTRE ADD CONSTRAINT FK_CAIXA_MESTRE_ID_EMPRESA FOREIGN KEY (ID_EMPRESA) REFERENCES EMPRESAS (ID_EMPRESA);
ALTER TABLE CAIXA_MESTRE ADD CONSTRAINT FK_CAIXA_MESTRE_ID_USUARIO FOREIGN KEY (ID_USUARIO) REFERENCES USUARIOS (ID_USUARIO);

CREATE INDEX IX_CAIXA_MESTRE_DATA_ABERTURA ON CAIXA_MESTRE (DATA_ABERTURA);
CREATE INDEX IX_CAIXA_MESTRE_DATA_FECHAMENTO ON CAIXA_MESTRE (DATA_FECHAMENTO);
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------


