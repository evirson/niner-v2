CREATE TABLE CAIXA_DETALHE (
   ID_CAIXA INTEGER NOT NULL,
   ID_MOEDA INTEGER NOT NULL,
   ID_VENDA INTEGER,
   ID_LOTE_RECEBIMENTO INTEGER,
   ID_PLANO_CONTAS VARCHAR(13),
   VALOR FLOAT NOT NULL,
   TIPO_OPERACAO VARCHAR(2) NOT NULL CHECK (TIPO_OPERACAO IN ('RV', 'RP', 'DC', 'CC', 'TR')),
   CREDITO_DEBITO VARCHAR(1) NOT NULL CHECK (CREDITO_DEBITO IN ('C', 'D')),
   OBSERVACOES VARCHAR(200),
   LOCALIZADOR INTEGER GENERATED BY DEFAULT AS IDENTITY);
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

ALTER TABLE CAIXA_DETALHE ADD CONSTRAINT PK_CAIXA_DETALHE PRIMARY KEY (LOCALIZADOR);

ALTER TABLE CAIXA_DETALHE ADD CONSTRAINT FK_CAIXA_DETALHE_ID_CAIXA FOREIGN KEY (ID_CAIXA) REFERENCES CAIXA_MESTRE (ID_CAIXA);
ALTER TABLE CAIXA_DETALHE ADD CONSTRAINT FK_CAIXA_DETALHE_ID_MOEDA FOREIGN KEY (ID_MOEDA) REFERENCES MOEDAS (ID_MOEDA);

CREATE INDEX IX_CAIXA_DETALHE_ID_VENDA            ON CAIXA_DETALHE (ID_VENDA);
CREATE INDEX IX_CAIXA_DETALHE_ID_LOTE_RECEBIMENTO ON CAIXA_DETALHE (ID_LOTE_RECEBIMENTO);
CREATE INDEX IX_CAIXA_DETALHE_ID_PLANO_CONTAS     ON CAIXA_DETALHE (ID_PLANO_CONTAS);
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------


