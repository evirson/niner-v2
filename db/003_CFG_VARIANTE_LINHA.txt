CREATE TABLE CFG_VARIANTE_LINHA (
   ID_VARIANTE_LINHA INTEGER GENERATED BY DEFAULT AS IDENTITY,
   DESCRICAO VARCHAR(40) NOT NULL);
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

ALTER TABLE CFG_VARIANTE_LINHA ADD CONSTRAINT PK_CFG_VARIANTE_LINHA PRIMARY KEY (ID_VARIANTE_LINHA);

CREATE INDEX IX_CFG_VARIANTE_LINHA_DESCRICAO ON CFG_VARIANTE_LINHA (DESCRICAO);
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

