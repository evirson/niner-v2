CREATE TABLE PRODUTOS_BALANCO (
   ID_EMPRESA INTEGER NOT NULL,
   DATA_MOVIMENTO TIMESTAMP NOT NULL,
   CODIGO_BARRA VARCHAR(30) NOT NULL,
   QTD_CONTAGEM FLOAT NOT NULL,
   LOCALIZADOR INTEGER GENERATED BY DEFAULT AS IDENTITY);
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

ALTER TABLE PRODUTOS_BALANCO ADD CONSTRAINT PK_PRODUTOS_BALANCO PRIMARY KEY (LOCALIZADOR);

ALTER TABLE PRODUTOS_BALANCO ADD CONSTRAINT FK_PRODUTOS_BALANCO_ID_EMPRESA   FOREIGN KEY (ID_EMPRESA)   REFERENCES EMPRESAS       (ID_EMPRESA);
ALTER TABLE PRODUTOS_BALANCO ADD CONSTRAINT FK_PRODUTOS_BALANCO_CODIGO_BARRA FOREIGN KEY (CODIGO_BARRA) REFERENCES PRODUTOS_BARRA (CODIGO_BARRA);

CREATE INDEX IX_PRODUTOS_BALANCO_DATA_MOVIMENTO ON PRODUTOS_BALANCO (DATA_MOVIMENTO);
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

