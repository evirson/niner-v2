CREATE TABLE VENDAS_DEVOLUCAO (
   ID_DEVOLUCAO INTEGER NOT NULL,
   ID_EMPRESA INTEGER NOT NULL,
   DATA_DEVOLUCAO TIMESTAMP NOT NULL,
   ID_VENDA_CREDITO INTEGER,
   ID_VENDA_DEBITO INTEGER,
   ID_VALE_MERCADORIA VARCHAR(10) NOT NULL,
   VALE_USADO VARCHAR(1) NOT NULL CHECK (VALE_USADO IN ('S', 'N')));
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

ALTER TABLE VENDAS_DEVOLUCAO ADD CONSTRAINT PK_VENDAS_DEVOLUCAO_ID_DEVOLUCAO PRIMARY KEY (ID_DEVOLUCAO);

ALTER TABLE VENDAS_DEVOLUCAO ADD CONSTRAINT FK_VENDAS_DEVOLUCAO_ID_EMPRESA FOREIGN KEY (ID_EMPRESA) REFERENCES EMPRESAS (ID_EMPRESA);

CREATE INDEX IX_VENDAS_DEVOLUCAO_DATA_DEVOLUCAO     ON VENDAS_DEVOLUCAO (DATA_DEVOLUCAO);
CREATE INDEX IX_VENDAS_DEVOLUCAO_ID_VENDA_CREDITO   ON VENDAS_DEVOLUCAO (ID_VENDA_CREDITO);
CREATE INDEX IX_VENDAS_DEVOLUCAO_ID_VENDA_DEBITO    ON VENDAS_DEVOLUCAO (ID_VENDA_DEBITO);
CREATE INDEX IX_VENDAS_DEVOLUCAO_ID_VALE_MERCADORIA ON VENDAS_DEVOLUCAO (ID_VALE_MERCADORIA);
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

