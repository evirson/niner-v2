CREATE TABLE CFG_VARIANTE_COLUNA (
   ID_VARIANTE_COLUNA INTEGER GENERATED BY DEFAULT AS IDENTITY,
   DESCRICAO VARCHAR(10) NOT NULL);
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

ALTER TABLE CFG_VARIANTE_COLUNA ADD CONSTRAINT PK_CFG_VARIANTE_COLUNA PRIMARY KEY (ID_VARIANTE_COLUNA);

CREATE INDEX IX_CFG_VARIANTE_COLUNA_DESCRICAO ON CFG_VARIANTE_COLUNA (DESCRICAO);
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

