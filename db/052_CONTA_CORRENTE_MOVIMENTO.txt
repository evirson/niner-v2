CREATE TABLE CONTA_CORRENTE_MOVIMENTO (
   ID_CONTA_CORRENTE VARCHAR(20) NOT NULL,
   DATA_MOVIMENTO TIMESTAMP NOT NULL,
   NUMERO_DOCUMENTO VARCHAR(25) NOT NULL,
   CREDITO_DEBITO VARCHAR(1) NOT NULL CHECK (CREDITO_DEBITO IN ('C', 'D')),
   COMPENSADO VARCHAR(1) NOT NULL CHECK (COMPENSADO IN ('S', 'N')),
   VALOR FLOAT NOT NULL,
   OBSERVACAO VARCHAR(1000),
   LOCALIZADOR INTEGER GENERATED BY DEFAULT AS IDENTITY);
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

ALTER TABLE CONTA_CORRENTE_MOVIMENTO ADD CONSTRAINT PK_CONTA_CORRENTE_MOVIMENTO PRIMARY KEY (LOCALIZADOR);

ALTER TABLE CONTA_CORRENTE_MOVIMENTO ADD CONSTRAINT FK_CONTA_CORRENTE_MOVIMENTO_ID_CONTA_CORRENTE FOREIGN KEY (ID_CONTA_CORRENTE) REFERENCES CONTA_CORRENTE (ID_CONTA_CORRENTE);

CREATE INDEX IX_CONTA_CORRENTE_MOVIMENTO_DATA_MOVIMENTO   ON CONTA_CORRENTE_MOVIMENTO (DATA_MOVIMENTO);
CREATE INDEX IX_CONTA_CORRENTE_MOVIMENTO_NUMERO_DOCUMENTO ON CONTA_CORRENTE_MOVIMENTO (NUMERO_DOCUMENTO);
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

