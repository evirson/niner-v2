CREATE TABLE VENDAS (
   ID_VENDA INTEGER NOT NULL,
   ID_EMPRESA INTEGER NOT NULL,
   ID_CLIENTE INTEGER NOT NULL,
   DATA_VENDA TIMESTAMP NOT NULL,
   TIPO_OPERACAO VARCHAR(1) NOT NULL CHECK (TIPO_OPERACAO IN ('V', 'D')));   -- V = VENDA / D = DEVOLUCAO
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

ALTER TABLE VENDAS ADD CONSTRAINT PK_VENDAS_ID_VENDA PRIMARY KEY (ID_VENDA);

ALTER TABLE VENDAS ADD CONSTRAINT FK_VENDAS_ID_EMPRESA FOREIGN KEY (ID_EMPRESA) REFERENCES EMPRESAS (ID_EMPRESA);
ALTER TABLE VENDAS ADD CONSTRAINT FK_VENDAS_ID_CLIENTE FOREIGN KEY (ID_CLIENTE) REFERENCES CLIENTES (ID_CLIENTE);

CREATE INDEX IX_VENDAS_DATA_VENDA ON VENDAS (DATA_VENDA);
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

