CREATE TABLE PRODUTOS_IMAGENS (
   ID_PRODUTO INTEGER NOT NULL,
   INDICE INTEGER NOT NULL,
   IMAGEM VARCHAR(200) NOT NULL);
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

ALTER TABLE PRODUTOS_IMAGENS ADD CONSTRAINT PK_PRODUTOS_IMAGENS PRIMARY KEY (ID_PRODUTO, INDICE);

ALTER TABLE PRODUTOS_ESTOQUE ADD CONSTRAINT FK_PRODUTOS_IMAGENS_ID_PRODUTO FOREIGN KEY (ID_PRODUTO) REFERENCES PRODUTOS (ID_PRODUTO);
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

