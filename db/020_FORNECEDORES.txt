CREATE TABLE FORNECEDORES (
   ID_FORNECEDOR INTEGER GENERATED BY DEFAULT AS IDENTITY,
   RAZAO_SOCIAL VARCHAR(100) NOT NULL,
   CNPJ VARCHAR(20),
   INSCRICAO VARCHAR(20),
   ENDERECO VARCHAR(100),
   NUMERO VARCHAR(10),
   BAIRRO VARCHAR(40),
   CIDADE VARCHAR(40),
   ESTADO VARCHAR(2),
   CEP VARCHAR(8),
   TELEFONE VARCHAR(20),
   CONTATO VARCHAR(50),
   EMAIL VARCHAR(100),
   ID_PLANO_CONTAS VARCHAR(13) NOT NULL);
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

ALTER TABLE FORNECEDORES ADD CONSTRAINT PK_FORNECEDORES PRIMARY KEY (ID_FORNECEDOR);

ALTER TABLE FORNECEDORES ADD CONSTRAINT FK_FORNECEDORES_PLANO_CONTAS FOREIGN KEY (ID_PLANO_CONTAS) REFERENCES CFG_PLANO_CONTAS (ID_PLANO_CONTAS);

CREATE INDEX IX_FORNECEDORES_RAZAO_SOCIAL ON FORNECEDORES (RAZAO_SOCIAL);
CREATE INDEX IX_FORNECEDORES_CNPJ ON FORNECEDORES (CNPJ);
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------


