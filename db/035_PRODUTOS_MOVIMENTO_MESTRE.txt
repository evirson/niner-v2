CREATE TABLE PRODUTOS_MOVIMENTO_MESTRE (
   ID_MOVIMENTO INTEGER NOT NULL,
   TIPO_MOVIMENTO INTEGER NOT NULL CHECK (TIPO_MOVIMENTO IN (01, 02, 03, 04, 05)), -- 1 - COMPRA  /  2 - TRANSFERENCIA  /  3 - DEVOLUCAO DE VENDA  /  4 - AJUSTE DE ESTOQUE / 5 - VENDA
   DATA_MOVIMENTO TIMESTAMP NOT NULL,
   ID_EMPRESA INTEGER NOT NULL,
   ID_FORNECEDOR INTEGER,
   ID_TRANSFERENCIA INTEGER,
   ID_DEVOLUCAO INTEGER,
   ID_VENDA INTEGER,
   NOTA_FISCAL INTEGER DEFAULT 0);
------------------------------------------------------------------------------------------------------------------------------------------------------------

ALTER TABLE PRODUTOS_MOVIMENTO_MESTRE ADD CONSTRAINT PK_PRODUTOS_MOVIMENTO_MESTRE PRIMARY KEY (ID_MOVIMENTO);

ALTER TABLE PRODUTOS_MOVIMENTO_MESTRE ADD CONSTRAINT FK_PRODUTOS_MOVIMENTO_MESTRE_ID_EMPRESA    FOREIGN KEY (ID_EMPRESA)    REFERENCES EMPRESAS         (ID_EMPRESA);
ALTER TABLE PRODUTOS_MOVIMENTO_MESTRE ADD CONSTRAINT FK_PRODUTOS_MOVIMENTO_MESTRE_ID_FORNECEDOR FOREIGN KEY (ID_FORNECEDOR) REFERENCES FORNECEDORES     (ID_FORNECEDOR);
ALTER TABLE PRODUTOS_MOVIMENTO_MESTRE ADD CONSTRAINT FK_PRODUTOS_MOVIMENTO_MESTRE_ID_DEVOLUCAO  FOREIGN KEY (ID_DEVOLUCAO)  REFERENCES VENDAS_DEVOLUCAO (ID_DEVOLUCAO);
ALTER TABLE PRODUTOS_MOVIMENTO_MESTRE ADD CONSTRAINT FK_PRODUTOS_MOVIMENTO_MESTRE_ID_VENDA      FOREIGN KEY (ID_VENDA)      REFERENCES VENDAS           (ID_VENDA);

CREATE INDEX IX_PRODUTOS_MOVIMENTO_MESTRE_DATA_MOVIMENTO   ON PRODUTOS_MOVIMENTO_MESTRE (DATA_MOVIMENTO);
CREATE INDEX IX_PRODUTOS_MOVIMENTO_MESTRE_ID_FORNECEDOR    ON PRODUTOS_MOVIMENTO_MESTRE (ID_FORNECEDOR);
CREATE INDEX IX_PRODUTOS_MOVIMENTO_MESTRE_ID_TRANSFERENCIA ON PRODUTOS_MOVIMENTO_MESTRE (ID_TRANSFERENCIA);
CREATE INDEX IX_PRODUTOS_MOVIMENTO_MESTRE_ID_DEVOLUCAO     ON PRODUTOS_MOVIMENTO_MESTRE (ID_DEVOLUCAO);
CREATE INDEX IX_PRODUTOS_MOVIMENTO_MESTRE_ID_VENDA         ON PRODUTOS_MOVIMENTO_MESTRE (ID_VENDA);
CREATE INDEX IX_PRODUTOS_MOVIMENTO_MESTRE_NOTA_FISCAL      ON PRODUTOS_MOVIMENTO_MESTRE (NOTA_FISCAL);
------------------------------------------------------------------------------------------------------------------------------------------------------------

