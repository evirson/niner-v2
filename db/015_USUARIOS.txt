CREATE TABLE USUARIOS (
   ID_USUARIO INTEGER GENERATED BY DEFAULT AS IDENTITY,
   USUARIO_ATIVO VARCHAR(1) NOT NULL CHECK (USUARIO_ATIVO IN ('S','N')),
   USUARIO_ADMINISTRADOR VARCHAR(1) NOT NULL CHECK (USUARIO_ADMINISTRADOR IN ('S','N')),
   NOME_USUARIO VARCHAR(50) NOT NULL,
   EMAIL VARCHAR(100) NOT NULL,
   SENHA VARCHAR(100) NOT NULL);
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

ALTER TABLE USUARIOS ADD CONSTRAINT PK_USUARIOS PRIMARY KEY (ID_USUARIO);

CREATE INDEX IX_USUARIOS_LOGIN ON USUARIOS (EMAIL, SENHA);
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------


