CREATE TABLE PRODUTOS_ESTOQUE (
   ID_EMPRESA INTEGER NOT NULL,
   CODIGO_BARRA VARCHAR(30) NOT NULL,
   QTD_ESTOQUE FLOAT DEFAULT 0);
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

ALTER TABLE PRODUTOS_ESTOQUE ADD CONSTRAINT PK_PRODUTOS_ESTOQUE PRIMARY KEY (CODIGO_BARRA, ID_EMPRESA);

ALTER TABLE PRODUTOS_ESTOQUE ADD CONSTRAINT FK_PRODUTOS_ESTOQUE_ID_EMPRESA   FOREIGN KEY (ID_EMPRESA)   REFERENCES EMPRESAS       (ID_EMPRESA);
ALTER TABLE PRODUTOS_ESTOQUE ADD CONSTRAINT FK_PRODUTOS_ESTOQUE_CODIGO_BARRA FOREIGN KEY (CODIGO_BARRA) REFERENCES PRODUTOS_BARRA (CODIGO_BARRA);
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

