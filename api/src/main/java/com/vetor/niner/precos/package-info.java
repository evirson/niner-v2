/**
 * Módulo <b>precos</b> — política de preço por canal. O ERP é master do preço (P1);
 * os canais são réplicas. Dinheiro em {@code NUMERIC}/{@code BigDecimal} (P7).
 * Classes entram com as migrations de domínio (V013+).
 */
package com.vetor.niner.precos;
